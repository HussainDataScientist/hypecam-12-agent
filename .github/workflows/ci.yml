/* GENERATED_BY: Antigravity DevOpsAgent */
name: CI

on: [push, pull_request]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - uses: pnpm/action-setup@v2
      - run: pnpm install
      - run: pnpm build
      - run: pnpm test
      - run: node scripts/capture-storybook-snapshots.js
      - run: node scripts/compare-snapshots.js
